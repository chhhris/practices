

def matching_parens(str)
  # go through each char in str
  # save parens w. index into a queue
  # ()(((()))(()))()
  # count opens
  # count closes
  # up to target

  # e.g. 3 opens
  # at end,
end

str = "Sometimes (when I nest them (my parentheticals) too much (like this (and this))) they get confusing."
puts matching_parens(str)